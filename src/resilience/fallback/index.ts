/**
 * `forge/resilience/fallback` — substitute a secondary result when the
 * primary operation fails.
 *
 * @module
 */

export { fallback } from "./fallback";
export type {
  FallbackHandler,
  FallbackOptions,
  FallbackPolicy,
  FallbackPredicate,
} from "./types";
