/**
 * Forge — the boring infrastructure layer your business logic deserves.
 *
 * Submodules:
 * - `forge/telemetry` — unified logs, metrics, and traces (this PR ships
 *   the scaffold, context propagation, and structured logging).
 *
 * @module
 */

export {};
